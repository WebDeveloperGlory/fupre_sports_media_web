import { apiClient } from "@/lib/config/client";

export const userApi = {
    // getAll: () => apiClient.get<PaginatedResponse<User>>('/users'),
    // getById: (id: string) => apiClient.get<User>(`/users/${id}`),
    // create: (data: CreateUserDto) => apiClient.post<User, CreateUserDto>('/users', data),
    // update: (id: string, data: Partial<CreateUserDto>) => apiClient.put<User>(`/users/${id}`, data),
    // delete: (id: string) => apiClient.delete<void>(`/users/${id}`)
};